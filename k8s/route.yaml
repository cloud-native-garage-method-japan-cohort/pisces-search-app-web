apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: pisces-search-app-web
spec:
  tls:
    termination: edge
  to:
    name: pisces-search-app-web
    kind: Service
    weight: 100
  wildcardPolicy: None
